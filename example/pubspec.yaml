name: intyx_example
description: Intyx Dynamic Widget paketinin kullanımını gösteren örnek uygulama.
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intyx_dynamic_widget:
    path: ../flutter_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
